name: Update Issue Status on PR

on:
  pull_request:
    # Akcja uruchomi się przy otwarciu, wyjściu z draftu i powrocie do draftu
    types: [opened, ready_for_review, reopened, converted_to_draft]

jobs:
  update-issue-status:
    runs-on: ubuntu-latest

    steps:
      - name: Extract Issue ID from PR branch name
        id: get_issue
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const branchName = context.payload.pull_request.head.ref;
            console.log(`Gałąź PR-a: ${branchName}`);
            
            const match = branchName.match(/\b(\d+)\b/);
            if (!match) {
              console.log("Nie znaleziono numeru zadania w nazwie gałęzi. Pomijam.");
              return;
            }
            
            const issueNum = parseInt(match[1], 10);
            
            try {
              const { data: issue } = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNum
              });
              
              core.setOutput('node_id', issue.node_id);
              console.log(`Znaleziono Issue #${issueNum}. Przekazuję do tablicy...`);
            } catch (error) {
              console.log(`Zgłoszenie #${issueNum} nie istnieje.`);
            }

      - name: Determine Target Column
        # Ustawiamy zmienną w zależności od tego, czy PR to Draft
        id: status
        run: |
          if [ "${{ github.event.pull_request.draft }}" == "true" ]; then
            echo "STATUS_VALUE=In Progress" >> $GITHUB_ENV
          else
            echo "STATUS_VALUE=In Review" >> $GITHUB_ENV
          fi

      - name: Update Project Status
        if: steps.get_issue.outputs.node_id != ''
        uses: leonsteinhaeuser/project-beta-automations@v2.2.1
        with:
          gh_token: ${{ secrets.PAT_TOKEN }}
          organization: KN-IGNITe
          project_id: 4
          resource_node_id: ${{ steps.get_issue.outputs.node_id }}
          status_value: ${{ env.STATUS_VALUE }}

